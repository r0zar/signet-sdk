/**
 * Fallback notification handler for when the extension isn't available
 */
export declare function showFallbackNotification(type: 'info' | 'success' | 'error', title: string, message: string): void;
